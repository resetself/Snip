# 更新日志

## 未发布

### 新增
- ✨ **长截图支持从起始选区向两端扩展**
  - 默认向下截图后，可回到起始位置并继续向上补充截图和标注
  - 已捕获范围内的反向滚动只更新 Vision 配准锚点，不重复拼图
  - 向顶部追加内容不会移动已有图片或标注；最终导出只按底部新增高度平移标注

### 修复
- 🐛 **修复鼠标位于标注层时长截图滚动卡住的问题**
  - 浏览态下选区窗口原生穿透，覆盖层不再监听或注入滚轮事件
  - 标注工具保持连续激活；只监听的事件 tap 在滚轮分发前临时暂停窗口交互，停止滚动后恢复原工具
- 🐛 **修复长截图滚动时标注层与页面不同步和抖动的问题**
  - 统一标注层内外滚轮事件，由 global monitor 观察页面实际收到的滚动
  - 标注层内改用窗口穿透和 HID 投递，替代不能可靠驱动页面滚动的 `postToPid`
  - 主线程直接处理滚动状态，并按显示器物理像素对齐标注偏移
  - Vision 配准失败时不再使用未经确认的滚轮距离追加长图
  - 详见 [长截图实现说明](SCROLL_CAPTURE_IMPLEMENTATION.md#17-标注层滚动抖动问题记录)
- 🐛 **修复截图时菜单和浮动贴图系统阴影消失的问题**
  - 根本原因：截图遮罩激活后才采集，导致菜单关闭且 ScreenCaptureKit 内容帧不包含可靠的窗口 framing 阴影
  - 解决方案：在创建截图遮罩前获取每个显示器的 WindowServer 合成图像，并将预采集图像注入选择界面
  - 浮动贴图继续使用 macOS 原生 `hasShadow`，移除内容视图中的模拟边框和阴影
  - 增加 ScreenCaptureKit 回退路径，并兼容 macOS 15 对 `CGWindowListCreateImage` 的 obsolete 标记
  - 详见 [截图时机与浮动窗口系统阴影](SCREEN_CAPTURE_TIMING_AND_SHADOW.md)

## [1.2] - 2026-01-14

### 新增
- ✨ **滚动长截图功能完善**
  - 基于滚轮监控的自动捕获（步长 = 选区高度）
  - 智能图片拼接（自动检测重叠并去重）
  - 长截图完成后显示浮动贴图窗口
  - 支持保存或复制长截图
  - 工具栏自适应宽度（根据按钮数量自动调整）

### 改进
- 🎨 优化工具栏布局，支持自适应宽度
- ⚡ 简化拼接逻辑（只有30行代码）
- 🐛 修复最后一张图片捕获超时问题（改为后台线程）
- 🐛 修复图片拼接重叠和缺失问题
- 📝 完善长截图使用文档

### 技术细节
- 使用 `NSEvent.addGlobalMonitorForEvents` 监听全局滚轮事件
- 累计滚动距离达到步长时自动截图
- 前 N-1 张图片直接拼接，最后一张检测重叠并裁剪
- 长截图模式下窗口缩小到选区+工具栏区域
- 保存屏幕绝对坐标避免窗口缩放导致的位置偏移

---

## [1.1] - 2026-01-13

### 修复
- 🐛 **修复 `Cmd+S` 保存功能崩溃问题**（`EXC_BREAKPOINT`）
  - 根本原因：在 `accessory` 模式下创建 `NSSavePanel` 会崩溃
  - 解决方案：先切换到 `regular` 模式，等待 0.2 秒后再创建面板
- 🐛 修复应用策略切换导致保存对话框无响应
- 🐛 更新废弃的通知 API（`NSUserNotification` → `UNUserNotificationCenter`）
- 🐛 添加完整的错误处理和用户提示

### 新增
- ✨ 支持保存为 JPEG 格式（原有 PNG）
- ✨ 保存成功后显示系统通知
- ✨ 保存失败时显示详细错误信息
- ✨ 优化内存管理（使用 `autoreleasepool`）

### 改进
- ⚡ 优化应用策略切换流程（确保不崩溃）
- 📝 更新 README 使用说明
- 📝 添加保存功能修复文档
- 📝 添加技术要点速查文档

### 技术细节
- 在应用启动时请求通知权限
- 保存图片时使用后台线程处理
- 根据文件扩展名自动选择图片格式
- 保存对话框默认位置为桌面
- 保存完成后自动恢复 `accessory` 模式

---

## 历史版本

### 已修复的问题
- ✅ 截图后 ESC 取消导致崩溃
- ✅ 浮动窗口空白无法拖动
- ✅ 快捷键失效
- ✅ 内存泄漏（截图后内存不释放）
- ✅ 窗口生命周期管理问题
- ✅ Cmd+S 保存功能崩溃

当前架构、关键实现和发布验证要求统一记录在 [开发与发布文档](DEVELOPMENT.md) 及两份专题实现文档中。
